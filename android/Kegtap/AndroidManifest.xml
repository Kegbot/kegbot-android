<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="org.kegbot.app"
    android:versionCode="2"
    android:versionName="1.0 beta 2" >

    <uses-sdk
        android:minSdkVersion="14"
        android:targetSdkVersion="14" />

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.WAKE_LOCK"/>
    
    <uses-feature android:name="android.hardware.usb.host" android:required="true" />
    <uses-feature android:name="android.hardware.screen.landscape" android:required="true" />
    
    <supports-screens
        android:smallScreens="false"
        android:normalScreens="false"
        android:largeScreens="false"
        android:xlargeScreens="true"
        />
    
    <application
        android:hardwareAccelerated="true"
        android:icon="@drawable/icon"
        android:label="@string/app_name"
        android:logo="@drawable/kegbot_logo"
        android:theme="@android:style/Theme.Holo" >

        <!-- ########## -->
        <!-- ACTIVITIES -->
        <!-- ########## -->


        <activity
            android:name=".KegtapLauncherActivity"
            android:label="@string/app_name"
            android:launchMode="singleTop"
            android:screenOrientation="sensorLandscape"
            android:windowSoftInputMode="stateAlwaysHidden|adjustPan" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.LAUNCHER" />

                <action android:name="android.hardware.usb.action.USB_DEVICE_ATTACHED" />
                <action android:name="android.hardware.usb.action.USB_DEVICE_DETACHED" />
            </intent-filter>

            <meta-data
                android:name="android.hardware.usb.action.USB_DEVICE_ATTACHED"
                android:resource="@xml/device_filter" />
        </activity>
        <activity
            android:name=".KegtapActivity"
            android:label="@string/app_name"
            android:launchMode="singleTop"
            android:screenOrientation="sensorLandscape"
            android:windowSoftInputMode="stateAlwaysHidden|adjustPan" >
        </activity>
        <activity
            android:name=".setup.SetupActivity"
            android:label="@string/app_name"
            android:launchMode="singleTop"
            android:screenOrientation="sensorLandscape">
        </activity>
        <activity
            android:name=".PourInProgressActivity"
            android:label="@string/app_name"
            android:launchMode="singleTop"
            android:screenOrientation="sensorLandscape"
            android:windowSoftInputMode="stateHidden|adjustPan" />
        <activity
            android:name=".DrinkerSelectActivity"
            android:label="@string/app_name"
            android:launchMode="singleTop"
            android:noHistory="true"
            android:screenOrientation="sensorLandscape"
            android:windowSoftInputMode="stateHidden|adjustPan"  >
        </activity>
        <activity
            android:name=".SettingsActivity"
            android:label="@string/settings_name" />
        <activity
            android:name=".EnterPinActivity"
            android:label="@string/app_name" />
        <activity
            android:name=".AuthenticatingActivity"
            android:label="@string/app_name"
            android:launchMode="singleTop"
            android:theme="@android:style/Theme.Translucent.NoTitleBar"
            android:noHistory="true" />
        
<!-- ################### -->
        <!-- BROADCAST RECEIVERS -->
        <!-- ################### -->

        <receiver android:name=".PourInProgressReceiver" >
            <intent-filter android:priority="1" >
                <action android:name="org.kegbot.action.POUR_START" />
            </intent-filter>
        </receiver>
        <receiver android:name=".UserAuthedReceiver" >
            <intent-filter android:priority="1" >
                <action android:name="org.kegbot.action.AUTH_BEGIN" />
                <action android:name="org.kegbot.action.AUTH_FAIL" />
                <action android:name="org.kegbot.action.USER_AUTHED" />
                </intent-filter>
        </receiver>

        <!-- ######## -->
        <!-- SERVICES -->
        <!-- ######## -->


        <!-- KegbotCoreService -->
        <service android:name=".service.KegbotCoreService" />

        <!-- KegbotApiService -->
        <service android:name=".service.KegbotApiService" />

        <!-- KegbotHardwareService -->
        <service android:name=".service.KegbotHardwareService" />

        <!-- KegbotHardwareService -->
        <service android:name=".service.KegbotSoundService" />

        <!-- KegboardService -->
        <service android:name=".service.KegboardService" >
        </service>
    </application>

</manifest>